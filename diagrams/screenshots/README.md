# Screenshots

This folder contains screenshots captured from the AWS console while building the **AWS VPC Traffic Flow & Security Project**.

Suggested files:

- `vpc-architecture.png` – VPC and subnet layout.
- `route-table.png` – Route table with `0.0.0.0/0` to Internet Gateway.
- `security-group.png` – Security Group inbound/outbound rules.
- `nacl-rules.png` – Network ACL rules for the subnet.
